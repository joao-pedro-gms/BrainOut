name: Deploy wireframes to GitHub Pages

# Faz deploy de `docs/wireframes/` para GitHub Pages sempre que houver
# push em `main` que altere arquivos nessa pasta, ou manualmente via
# workflow_dispatch. O build é mínimo: copia os arquivos estáticos
# para um diretório de artifact e o action `actions/deploy-pages`
# publica.

on:
  push:
    branches: [main]
    paths:
      - 'docs/wireframes/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Sem concorrência com outros deploys — Pages suporta apenas 1 in-flight.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build wireframes
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Copy wireframes to artifact directory
        run: |
          set -e
          STAGE="$RUNNER_TEMP/brainout-pages"
          rm -rf "$STAGE"
          mkdir -p "$STAGE"

          # O site publicado fica em /<repo>/docs/ dentro do GitHub Pages.
          mkdir -p "$STAGE/docs"
          cp -r docs/wireframes/. "$STAGE/docs/"

          # .nojekyll garante que GitHub Pages sirva os arquivos como
          # estáticos puros, sem processar Markdown/Liquid/Jekyll.
          touch "$STAGE/.nojekyll"

          # Adiciona index.html na raiz do site que redireciona para
          # /<repo>/docs/index.html (evita 404 quando o usuário acessa
          # apenas https://joao-pedro-gms.github.io/BrainOut/).
          cat > "$STAGE/index.html" <<'HTML'
          <!DOCTYPE html>
          <html lang="pt-BR">
          <head>
            <meta charset="UTF-8" />
            <title>BrainOut — Wireframes</title>
            <meta http-equiv="refresh" content="0; url=./docs/index.html" />
            <link rel="canonical" href="./docs/index.html" />
          </head>
          <body>
            <p>Redirecionando para <a href="./docs/index.html">wireframes</a>…</p>
          </body>
          </html>
          HTML

          echo "Arquivos prontos para deploy:"
          find "$STAGE" -maxdepth 3 -type f | sort
          echo "STAGE=$STAGE" >> "$GITHUB_ENV"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.STAGE }}

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4